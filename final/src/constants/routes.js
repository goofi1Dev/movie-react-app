const home = "/";
const movie = "/movie";
const signin = "/Sign_in";
const signup = "/Sign_up";
const profile = "/profile-_-";
const routes = {
  home,
  movie,
  signin,
  signup,
  profile,
};

export default routes;
